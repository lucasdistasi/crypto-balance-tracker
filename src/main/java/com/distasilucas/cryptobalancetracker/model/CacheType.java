package com.distasilucas.cryptobalancetracker.model;

public enum CacheType {
    USER_CRYPTOS_CACHES,
    CRYPTOS_CACHES,
    PLATFORMS_CACHES,
    GOALS_CACHES,
    PRICE_TARGETS_CACHES,
    INSIGHTS_CACHES
}
